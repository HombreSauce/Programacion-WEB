# Estructura
- db: Lógica de la base de datos
  - queries: Consultas SQL (usadas por sqlc)
  - schema: Definición del esquema de la base de datos.
  - sqlc: Código Go generado por sqlc.
- "main.go": Entrada de la aplicación.
- "docker-compose.yaml": Configuración de PostgreSQL en contenedor
- "sqlc.yaml": Configuración de sqlc

# Requisitos
- Go 1.20+
- sqlc
- Docker

# Ejecución
- Moverse desde la terminal a la carpeta contenedora del archivo Makefile
- Ejecutar en terminal "make run" esto levantará el docker compose, generará el sqlc y correrá el archivo main.go una vez que 
  se pueda conectar a la base de datos.